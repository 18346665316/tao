布隆过滤器
	1. 将url(或其他样本)分别经过k个哈希函数进行计算，得到 k个 哈希值[h1,h2,h3...hk]
	2. 将k个哈希值映射到redis的位数组区间(区间大小[0,2^32])上,得到 k个 对应的下标值[v1,v2,v3...vk]
	3. 判断k个下标值，如果其中某个下标对应的数据是0，说明该url不存在，只有所有下标位置对应的数据都是1，才能证明该url已经存在了
	4. 如果需要存储该url，只需将下标位置对应的数据全部改为1即可

